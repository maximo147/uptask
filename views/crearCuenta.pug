doctype html
html(lang="es")
    head
        include partials/header    
    body.login
        .contenedor-formulario 
            h1 #{titulo}
            if error
                each val in error
                    div.alerta.error= val.message
            form#formulario.caja.caja-login(action="/usuario/crear-usuario", method="post")
                .campo 
                    label(for="email") E-mail: 
                    input(type="email", name="email", placeholder="Tu Email" value= email)
                .campo 
                    label(for="password") Password
                    input(type="password" name="password" placeholder="Tu password" value= password required)
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo.acciones
                    a(href="/auth/iniciar-sesion") Iniciar sesión
                    a(href="/usuario/reestablecer") ¿Olvidaste tu contraseña?